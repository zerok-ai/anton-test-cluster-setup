apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: load-test-highload-service-monitor
  namespace: load-test
  labels:
    app: myapp
    load: high
spec:
  selector:
    matchLabels:
      app: myapp
      load: high
  endpoints:
  - port: m-http
    path: /metrics
  namespaceSelector:
    matchNames:
      - app
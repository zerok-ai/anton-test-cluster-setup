apiVersion: v1
kind: Service
metadata:
  name: load-test-service-highload
  namespace: load-test
  labels:
    app: myapp
    load: high
spec:
  selector:
    app: myapp
    load: high
  type: ClusterIP # LoadBalancer # NodePort # 
  ports:
    - protocol: TCP
      name: m-http
      port: 80
      targetPort: 3000

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: load-test-highload-service-monitor
  labels:
    app: myapp
    load: high
spec:
  selector:
    matchLabels:
      app: myapp
      load: high
  endpoints:
  - port: m-http
    path: /metrics
  namespaceSelector:
    matchNames:
      - app